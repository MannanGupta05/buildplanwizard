services:
  - type: web
    name: buildplanwizard
    env: python
    plan: free
    aptPackages:
      - poppler-utils
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web.app:app
